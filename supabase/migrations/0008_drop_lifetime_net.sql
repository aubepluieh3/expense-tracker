-- =============================================================================
-- 0008_drop_lifetime_net.sql — 앱 사용 이후 누적 RPC 제거
--
-- 통계 화면에서 `앱 사용 이후 누적` 줄을 뺐다 (기획서 §3.7). 이 함수는 그 줄
-- 하나만을 위해 있었고 호출부가 사라졌다.
--
-- 뺀 이유는 화면 쪽 문제였다: 이 화면의 기간 축은 달력월과 월급 주기 둘인데
-- 전 기간 수지는 어느 쪽에도 속하지 않아 축을 바꿔도 혼자 그대로 있었고,
-- 달력월 요약이 이 화면에 온 뒤로는 "7월 남은 금액" 과 같은 숫자로 나란히 떴다.
--
-- 남겨 두면 아무도 부르지 않는 실행 권한이 anon·authenticated 에 계속 열려 있다.
-- cascade 를 쓰지 않는다 — 딸린 것이 있으면 실패하는 쪽이 맞다 (적용 시 확인:
-- 의존 객체 없음).
-- =============================================================================

drop function if exists public.get_lifetime_net();
