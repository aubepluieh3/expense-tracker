dist
node_modules
package-lock.json
*.tsbuildinfo
e2e/shots

# SQL 은 Prettier 가 다루지 않는다. 마이그레이션의 컬럼 정렬은 손으로 맞춘 것이다.
supabase/migrations

# 마크다운은 제외한다.
# Prettier 를 붙이면 기획서·설계서 600여 줄이 한 번에 재포맷된다 — 표 정렬과
# 줄바꿈 위치를 손으로 맞춘 한국어 산문이라, 얻는 것 없이 diff 만 만든다.
# 포매터의 값은 "사람이 다투지 않게 하는 것"이고, 문서 줄바꿈은 다툰 적이 없다.
*.md
