# SPA 폴백 (Cloudflare Pages).
#
# 정적 파일을 먼저 찾고, 없을 때만 이 규칙이 걸린다 — /guide.html 은 파일이
# 있으므로 그대로 나가고, /stats · /signup 처럼 라우터만 아는 주소는 index.html
# 이 받아서 React Router 가 처리한다.
#
# 이게 없으면 배포본에서 첫 요청이 404 다. 앱 안에서 이동할 때는 라우터가
# 처리하니 안 드러나지만, 주소를 직접 열거나 새로고침하면 깨진다. 특히
# 비밀번호 재설정과 이메일 인증은 **메일 링크가 곧 첫 요청**이라
# (/reset-password) 폴백이 없으면 기능 자체가 동작하지 않는다.
/*    /index.html   200
